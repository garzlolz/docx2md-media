# Docx2MD Media Converter

這是一個基於 Python 與 Pandoc 的工具，能將 `.docx` 檔案批次轉換為 Markdown，並完整提取高保真原始圖片。

## 快速開始

### 1. 環境需求

- **Python 3.13+**
- **Pandoc** (建議透過 Conda 安裝)

### 2. 安裝步驟

如果你使用 Anaconda:

```bash
conda env create -f environment.yml
conda activate py313
```
